spring:
  application:
    name: simple-crud
  profiles:
    active: ${PROFILE:dev}
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: ${PORT:8080}

logging:
  level:
    root: INFO